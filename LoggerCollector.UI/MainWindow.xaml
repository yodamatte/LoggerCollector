<Window x:Class="LoggerCollector.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoggerCollector" 
        xmlns:view="clr-namespace:LoggerCollector.UI.View" 
        xmlns:viewmodels="clr-namespace:LoggerCollector.UI.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <view:DatabaseConfigurationView DataContext="{Binding DatabaseConfigurationViewModel}" Grid.Column="0"/>
        <view:LoggerView DataContext="{Binding LoggerViewModel}" Grid.Column="1"/>
    </Grid>
</Window>
